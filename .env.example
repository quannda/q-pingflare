ADMIN_USER=admin
ADMIN_PASS=change-me
JWT_SECRET=change-me
ENCRYPTION_KEY=change-me
# DB_PATH=./data/pingflare.db
# PORT=3000
# NODE_ENV=production

# Removes the built-in login. Only behind a trusted proxy (Cloudflare Access, SSO).
# With this on, ADMIN_USER/ADMIN_PASS/JWT_SECRET above are optional.
# AUTH_DISABLED=true
